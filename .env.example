# このファイルをコピーして .env を作る。
# DB は "npm run db:up"（docker-compose）で起動する。下の値のまま繋がる。
DATABASE_URL="postgresql://stockdesk:stockdesk@localhost:5433/stockdesk"

# 生成: npx auth secret
AUTH_SECRET="replace-me"
